# 需求：会话列表按坐席隔离（坐席只看自己的，企业主看全部）

## 背景

当前 `GET /api/v1/conversations` 是租户全量共享：租户内任意坐席都能看到所有坐席的进行中/已结束会话。产品要求：

- **普通坐席（role=agent）**：只看到自己受理的会话
- **企业主/admin**：维持现状，看租户全量（admin 用的就是同一个坐席工作台 /agent/conversations）
- **排队会话（status=queued，无受理人）**：保持对所有坐席可见（见下文说明）

前端零改动——列表是数据驱动的，后端过滤后自动生效，无需新增字段或接口。

---

## 改动点 1（核心）：GET /api/v1/conversations 按角色过滤

在现有 `tenant_id` 过滤基础上追加：

```
role = 'agent'  →  assigned_agent_id = 当前用户 _id  OR  status = 'queued'
role = 'admin'  →  不追加条件（租户全量，现状不变）
```

**为什么保留 queued 全员可见**：排队会话是纯自动分配的暂态（坐席上线触发 redistribute 队列就分空了），没有"人工抢单"动作；且坐席全部离线时谁都看不到，只有"坐席在线但全满/分配失败"的边缘场景才会积压。保留可见让团队能感知积压，也避免大面积改动现有 e2e 断言。

**分配逻辑不动**：assignNewConversation / redistributeQueuedConversations / pickAvailableAgent 的候选筛选和"活跃数最少者优先"规则全部维持现状。

**预期行为变化**：
- 坐席 A 不再看到坐席 B 正在接待的会话和 B 的历史会话
- 未分配的排队会话所有坐席仍可见
- 转接后（assigned_agent_id 变更）新受理人可见、原受理人不再可见——预期行为
- 被停用坐席（is_active=false）名下的存量会话：admin 可见，其他坐席不可见——预期行为

---

## 改动点 2（建议，堵越权口子）：详情/消息读取接口加受理人校验

列表过滤后，坐席虽然看不到别人的会话，但直接调接口（改 URL 里的 id）仍可读取他人会话。建议以下接口统一加 **"受理人或 admin"** 校验（命中他人会话返回 403）：

- `GET /conversations/:id`（会话详情）
- `GET /conversations/:id/messages`（消息列表）
- 其他以 `:id` 操作会话的 REST 端点（逐个过一遍）

**Socket 侧同查**：
- `join_conversation` 事件：目前任意租户坐席应可 join 任意会话房间——加校验后非受理人 join 不了，才能收不到别人的实时消息
- `message_read` 事件：同样建议校验

**close 已有校验**（"仅受理人或管理员可结束会话"），不用动。`conversation_updated` 广播到租户坐席房间的机制不用动——其他坐席收到事件重拉列表后本来就查不到该会话，无害。

---

## 改动点 3（建议，与列表隔离对齐）：DELETE /conversations/:id 收紧

当前是租户内任意坐席可删任意 resolved 会话。列表隔离后坐席看不到别人的已结束会话，但直接调接口仍能删。建议同步收紧为 **"受理人或 admin"**，403 语义与改动点 2 一致。

---

## e2e 影响清单

111 项断言里需要检查的：

1. 凡用 agentToken（agent01）查 `GET /conversations` 断言"列表包含某会话"的地方：
   - 该会话分配给 agent01 → 不受影响
   - 分配给其他坐席（如 admin 受理的）→ 过滤后 agent01 查不到，断言需改用 adminToken 或调整预期
2. queued 相关断言（"坐席离线时会话为 queued"、"排队会话被自动分配"）：queued 保持全员可见，**不受影响**
3. 新增建议断言：
   - agent 查列表不含他人受理的会话（403 路径：直接 GET 他人会话详情 → 403）
   - agent 删除他人 resolved 会话 → 403
   - admin 查列表仍含全部会话

---

## 验证场景（手动）

1. 同租户两个坐席账号各开一个浏览器：各自只看到自己名下的会话；一方新进会话，另一方列表不出现
2. admin 账号登录：看到租户全部会话
3. 坐席全部离线 → 访客发起会话（queued）→ 坐席上线 → 自动分配后出现在该坐席列表
4. 回归删除功能：坐席删自己的已结束会话正常；删他人会话 → 403 + 前端红色错误提示
